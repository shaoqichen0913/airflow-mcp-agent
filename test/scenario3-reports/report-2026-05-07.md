# DAG Health Report — 2026-05-07

> Generated: 2026-05-07 (automated review window: 2026-05-06T00:00:00Z – 2026-05-07T23:59:59Z)

---

## Summary

| Metric | Value |
|---|---|
| DAGs monitored | 7 |
| Total runs (24h) | 5 |
| Succeeded | 2 |
| Failed | 3 |
| SLA breaches (> 10 min) | 0 |

---

## DAG Status

### ✅ `happy_path` — 2/2 succeeded (avg: ~3.4s)

- Run 1: `manual__2026-05-06T21:23:37.037287+00:00` — success in 3.63s
- Run 2: `manual__2026-05-07T09:56:27.844396+00:00` — success in 3.15s
- No issues detected. Healthy baseline.

---

### ⚠️ `failure_simple` — 0/1 runs succeeded

- Failed run: `manual__2026-05-06T21:39:14.759204+00:00`
- Failed task: `fail_task`
- Duration: ~0.85s (failed fast, no retries configured)
- Error: `ValueError: Simulated failure: upstream data source unavailable`
- Root cause: Deterministic `ValueError` raised in task Python callable; no retry configured (`max_tries=0`).
- Assessment: This is a hard, deterministic failure — retrying will not help without fixing the upstream data source dependency.

---

### ⚠️ `failure_with_retry` — 0/1 runs succeeded

- Failed run: `manual__2026-05-06T21:40:35.824892+00:00`
- Failed task: `flaky_task`
- Duration: ~17.67s (exhausted all 3 retries, try_number reached 4)
- Error (try 1): `ConnectionError: External API timeout: connection refused after 30s`
- Assessment: All 4 attempts (1 original + 3 retries) raised the same `ConnectionError`. The external API appears consistently unreachable — not a transient blip. Manual intervention required to investigate the external API endpoint availability before re-triggering.

---

### ⚠️ `upstream_downstream` — 0/1 runs succeeded

- Failed run: `manual__2026-05-06T21:43:25.608472+00:00`
- Task breakdown:

| Task | State | Note |
|---|---|---|
| `task_a` | success | Completed normally |
| `task_b` | failed | RuntimeError raised |
| `task_c` | upstream_failed | Never executed — blocked by task_b failure |

- Failed task: `task_b`
- Error: `RuntimeError: task_b: Transformation failed — schema mismatch in column 'event_ts'`
- Root cause: `task_b` encountered a schema mismatch on column `event_ts` during transformation. Airflow propagated the failure state upstream to `task_c` which was never scheduled (state: `upstream_failed`, `try_number=0`, no PID assigned — confirming it never ran).
- Dependency chain: `task_a` → `task_b` → `task_c`. `task_b`'s failure caused `task_c` to be marked `upstream_failed` automatically by Airflow's dependency resolver.

---

### ➖ `dag_code_bug` — no runs in last 24h

- 0 runs triggered in the review window.
- DAG is active and not paused; schedule is "external triggers only".

---

### ➖ `dag_transient_failure` — no runs in last 24h

- 0 runs triggered in the review window.
- DAG is active and not paused; schedule is "external triggers only".

---

### ➖ `long_running` — no runs in last 24h

- 0 runs triggered in the review window.
- DAG is active and not paused; schedule is "external triggers only".

---

## SLA Breaches

No SLA breaches detected. All completed runs finished well under the 10-minute threshold:

| DAG | Run | Duration |
|---|---|---|
| `happy_path` | manual__2026-05-06T21:23:37 | 3.63s |
| `happy_path` | manual__2026-05-07T09:56:27 | 3.15s |
| `failure_simple` | manual__2026-05-06T21:39:14 | 0.85s |
| `failure_with_retry` | manual__2026-05-06T21:40:35 | 17.67s |
| `upstream_downstream` | manual__2026-05-06T21:43:25 | 1.79s |

---

## Recommended Actions

1. **`failure_simple` — investigate upstream data source**
   - Error: `upstream data source unavailable`. Check whether the expected data source or connection is reachable. Fix the root cause before re-triggering. Consider adding retry logic if the dependency is intermittently available.

2. **`failure_with_retry` — investigate external API endpoint**
   - All 3 retries failed with `ConnectionError: External API timeout: connection refused after 30s`. The API is not a transient failure — it is consistently unreachable. Verify the API host/port, network routing, and whether credentials or certificates have expired. Do not re-trigger without confirming the API is available.

3. **`upstream_downstream` — fix schema mismatch in `task_b`**
   - Error: `schema mismatch in column 'event_ts'`. Review the upstream data schema and align the transformation logic in `task_b` to the actual column type/format. Once fixed, re-trigger the DAG run — `task_c` will automatically execute after `task_b` succeeds.

4. **`dag_code_bug`, `dag_transient_failure`, `long_running` — no action required today**
   - These DAGs have no runs in the 24h window. They are externally triggered; no scheduled activity was expected.

5. **Consider adding retry configuration to `failure_simple`**
   - Currently `max_tries=0`. If upstream data unavailability is expected to be transient in some cases, adding 1–2 retries with exponential backoff would prevent immediate hard failures.
